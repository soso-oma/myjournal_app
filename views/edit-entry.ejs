<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Edit Entry - MyJournal</title>
  <link rel="stylesheet" href="/styles.css">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
  <div class="container">
    <h1 class="center">Edit Your Entry</h1>
    <form method="POST" action="/edit/<%= entry._id %>">
      <input type="text" name="title" value="<%= entry.title %>" required placeholder="Title">
      <textarea name="content" rows="10" required placeholder="Write your journal entry..."><%= entry.content %></textarea>
      <button type="submit">Update Entry</button>
    </form>
    <div class="center" style="margin-top: 20px;">
      <a href="/dashboard">← Back to Dashboard</a>
    </div>
  </div>
</body>
</html>
